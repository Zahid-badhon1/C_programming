#include <stdio.h>
int main()
{
    int a;
    scanf("%d", &a);
    int arr[a];
    for (int i = 1; i <= a; i++)
    {
        scanf("%d", &arr[i]);
    }

    printf("%d %d %d", arr[5], arr[3], arr[1]);

    return 0;
}