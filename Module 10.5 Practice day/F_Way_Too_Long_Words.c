#include <stdio.h>
#include <limits.h>
int main()
{
    int n;
    char str;
    fgets(str, sizeof(str), stdin);
    printf("%s",str);
    // for (int i = 0; i < n; i++)
    // {
        
    // }

    return 0;
}